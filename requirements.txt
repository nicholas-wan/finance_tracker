# Only what the scripts import. Install with:
#   pip install -r requirements.txt
pypdf==6.16.2

# openpyxl is needed solely by scripts/migrate_from_xlsx.py, the one-time export
# from Finances.xlsx. The regular pipeline never reads the spreadsheet.
openpyxl==3.1.5
